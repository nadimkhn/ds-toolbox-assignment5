# Definition: git status

The `git status` command helps us identify whether local repository is ahead or behind of the remote repository.  

You can access the same informnation in JupyterLab, under the **Changges** tab on the Git extension panel.  