# Definition: git status

The `git status` command helps us identify whether local repository is in sync with the remote repository and determine whether we need to pull or push the changes.  

You can access the same informnation in JupyterLab, under the **Changges** tab on the Git extension panel.  